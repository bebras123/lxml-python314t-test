# Just for testing lxml

1. Create sample xml `python sample.py` --> example.xml
2. Run `docker compose build`
3. Run `docker compose run --rm python314-lxml bash`
4. Run `python3.14t run.py` or `python3.14 run.py` or `python3.13 run.py`
